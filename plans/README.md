# Implementation Plans

Generated by the improve skill on 2026-07-19 against commit `77bd4fc`. Execute one thin vertical slice at a time in the order below. Each executor must read its plan fully, honor STOP conditions, run all verification gates, and update its status here.

The maintainer explicitly chose not to add automated tests; every plan preserves that policy. Typecheck, lint, build, targeted static checks, and specified manual scenarios are the verification baseline.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| [001](001-reliable-title-renames.md) | Report document title saves truthfully | P1 | S | — | DONE |
| [002](002-minimize-realtime-logs.md) | Remove sensitive identifiers from routine realtime logs | P1 | S | — | DONE |
| [003](003-order-document-refreshes.md) | Prevent stale document-list refreshes | P1 | S | — | DONE |
| [004](004-order-collaborator-refreshes.md) | Prevent stale collaborator-list responses | P1 | S | — | DONE |
| [005](005-add-readiness-probe.md) | Add a PostgreSQL-aware readiness probe | P1 | S | — | DONE |
| [006](006-paginate-document-library.md) | Paginate the document library end to end | P2 | M | 003 | DONE |
| [007](007-extract-awareness-module.md) | Encapsulate awareness protocol state | P2 | M | — | TODO |
| [008](008-extract-document-runtime.md) | Encapsulate collaborative document lifecycle | P2 | L | 007 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (include one-line reason) | `REJECTED` (include rationale).

## Dependency and slicing notes

- Plans 001–005 are independent, complete vertical fixes. Land and verify each separately.
- Plan 006 requires plan 003 because paginated refresh and append requests must retain stale-response ownership rules.
- Plans 007 and 008 address the oversized Socket.IO module through two behavior-preserving deep-module slices. Execute them last to avoid repeated merge conflicts in the highest-churn file.
- Plan 008 requires plan 007 so awareness state is already outside the transport before the document lifecycle seam moves.
- Do not combine plans 007 and 008 into one change. Each has its own build gates and manual behavior check.

## Findings considered and rejected

- **Add automated tests and CI**: rejected by explicit maintainer decision; absence of tests is intentional. No plan may introduce a test framework or test files.
- **Routine patch/minor dependency upgrades**: not worth a dedicated migration; `npm audit --omit=dev --audit-level=high` reported no vulnerabilities and no core dependency was found at an urgent unsupported major.
- **Import support, revision history, and multi-instance coordination**: direction options, not current defects. Deferred until product/deployment needs justify their scope.

## Completion baseline

For every plan:

1. `npm run typecheck` exits 0.
2. `npm run lint` exits 0 with no warnings.
3. `npm run build` exits 0.
4. The plan-specific static/manual checks pass.
5. `git status --short` contains no files outside that plan's scope, except this status index.
