# Screenshots

This directory holds the images embedded in the repository root `README.md`.

Expected files:

| File | What it should show |
| --- | --- |
| `editor.png` | The editor with two collaborators' cursors visible |
| `dashboard.png` | The document list |

These must be captured manually against a running instance. Until `editor.png`
exists, the embed in the root `README.md` renders as broken alt text on GitHub.
